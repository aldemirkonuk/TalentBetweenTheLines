---
phase: 01-video-coordinates
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - read_the_floor/colab_read_the_floor.ipynb
  - read_the_floor/SETUP.md
files_produced_external:
  - prototypes/video_matchup.gif
  - read_the_floor/frames/still_*.png
autonomous: false
requirements: [REQ-video-to-coords]

user_setup:
  - service: roboflow
    why: "Hosted model inference for player detection + court keypoints (basketball-player-detection-3-ycjdo, basketball-court-detection-2) via the inference SDK get_model()"
    env_vars:
      - name: ROBOFLOW_API_KEY
        source: "Roboflow dashboard -> Settings -> API key; set as a Colab Secret (key icon), NEVER hardcoded in a cell"
    dashboard_config:
      - task: "Confirm access to the three Roboflow Universe datasets (player detection, court keypoints, jersey OCR)"
        location: "Roboflow Universe -> roboflow-jvuqo workspace"
  - service: colab
    why: "RF-DETR + SAM2 require a GPU; local Python is 3.9.6 (below sam2's 3.10+ minimum)"
    dashboard_config:
      - task: "Runtime -> Change runtime type -> T4 GPU; upload a 5-15s single-possession .mp4 clip to /content/clip.mp4"
        location: "colab.research.google.com"

must_haves:
  truths:
    - "Colab cell 3 is filled (RF-DETR detection + SAM2 tracking + SigLIP/UMAP/KMeans teams + optional SmolVLM2 jersey + keypoints->homography using BasketballCourtConfig in feet) and runs on one real clip producing frames_out"
    - "The skalski adapter converts that frames_out into the schema.py tracking table with NO schema fork (cell 4 unchanged), and schema.validate + the 6 geometry/presence asserts pass inline"
    - "The unchanged brain (matchup + Beaten Index + value + render) runs on the video-derived coordinates and produces a broadcast X-ray overlay GIF"
    - "Video-path numbers are sanity-checked vs the SportVU baseline; the GIF is saved to prototypes/ and stills to read_the_floor/frames/"
  artifacts:
    - path: "read_the_floor/colab_read_the_floor.ipynb"
      provides: "Cell 3 filled per the RESEARCH cell-3 structure; cell 4 inline schema+geometry asserts"
      contains: "frames_out"
    - path: "prototypes/video_matchup.gif"
      provides: "The headline real-clip X-ray overlay GIF (portfolio proof)"
    - path: "read_the_floor/frames/still_00.png"
      provides: "Saved overlay stills from the video run"
  key_links:
    - from: "read_the_floor/colab_read_the_floor.ipynb cell 3"
      to: "read_the_floor/basketball_court_config.py"
      via: "ViewTransformer target = vertices_ft() (FEET)"
      pattern: "vertices_ft|BASKETBALL_COURT_VERTICES_FT"
    - from: "read_the_floor/colab_read_the_floor.ipynb cell 4"
      to: "read_the_floor/adapters/skalski.py"
      via: "frame_to_rows over frames_out (unchanged)"
      pattern: "skalski\\.frame_to_rows"
    - from: "read_the_floor/colab_read_the_floor.ipynb cell 5"
      to: "read_the_floor/render.py"
      via: "render_gif on brain.run output"
      pattern: "render\\.render_gif"
---

<objective>
Fill Colab cell 3 with the RF-DETR + SAM2 + SigLIP/UMAP/KMeans + (optional) SmolVLM2 +
keypoints->homography pipeline, wire its `frames_out` through the UNCHANGED skalski
adapter -> schema -> brain -> render, run it on ONE real clip, sanity-check the numbers
against the SportVU baseline, and save the headline X-ray GIF to `prototypes/` and stills
to `frames/`.

Purpose: This is the critical unlock — the first real-footage render. The local spine is
already proven (plans 01-02) on a synthetic frames_out of the exact shape; this plan
supplies the real frames_out from real CV and lands the portfolio's real-clip proof.
Output: filled notebook, `prototypes/video_matchup.gif`, `frames/still_*.png`.

GPU-GATED: cell 3 runs only on a Colab T4 (local Python is 3.9.6, below sam2's 3.10+).
This plan is autonomous:false — its core tasks are human/Colab runs with concrete
observable acceptance artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-video-coordinates/01-RESEARCH.md
@.planning/phases/01-video-coordinates/01-VALIDATION.md
@.planning/intel/constraints.md
@.planning/intel/decisions.md

<interfaces>
<!-- The cell-3 output contract the adapter consumes (DO NOT change the adapter). -->

frames_out (cell 3 must produce) — list, one 5-tuple per frame:
```python
players     : sv.Detections   # RF-DETR player boxes; players.tracker_id = np.array(out_obj_ids)  (NOT None)
ball        : sv.Detections   # ball box(es); may be len 0
transformer : ViewTransformer # ViewTransformer(source=detected_px, target=vertices_ft())  -> feet
team_by_id  : dict[int,int]   # {tracker_id: 0|1}  from TeamClassifier (fit ONCE, predict per frame)
number_by_id: dict[int,str]   # {tracker_id: '23'} or {}  (optional)
```

From plan 01 — read_the_floor/basketball_court_config.py:
```python
def vertices_ft() -> np.ndarray (K,2) float32   # FEET, 0..94 x 0..50
KEYPOINT_NAMES : list[str]                       # parallel index map to reorder vs the model
```

From read_the_floor/adapters/skalski.py (UNCHANGED — cell 4 already calls it):
```python
per_frame = [skalski.frame_to_rows(i, *t) for i,t in enumerate(frames_out)]
df = skalski.build_table(per_frame, smooth=True)
```

Roboflow Universe dataset IDs (CON-pipeline-stack):
  player detection: roboflow-jvuqo/basketball-player-detection-3-ycjdo
  court keypoints : roboflow-jvuqo/basketball-court-detection-2
  jersey OCR      : roboflow-jvuqo/basketball-jersey-numbers-ocr
Pinned versions: rfdetr 1.3.0, supervision 0.28.0, umap-learn 0.5.12, sports@42c80c0, torch>=2.5.1 (sam2).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Wave-0 prerequisites — clip, Colab GPU, API key, keypoint ordering</name>
  <read_first>
    - read_the_floor/SETUP.md (Track B steps B1-B5)
    - .planning/phases/01-video-coordinates/01-RESEARCH.md ("Test Clip Sourcing", "Wave 0 Gaps", Open Question 1, Pitfall 7)
    - read_the_floor/basketball_court_config.py (KEYPOINT_NAMES — the ordering to confirm)
  </read_first>
  <what-built>
    The autonomous spine (plans 01-02) is done: BasketballCourtConfig in feet, the synthetic
    frames_out fixture, the pytest suite, run_video.py, validate_video.py — all green locally.
    These tasks are the things Claude literally cannot do: source a clip, hold a GPU session,
    hold a secret, and read the live model's keypoint ordering.
  </what-built>
  <how-to-verify>
    1. CLIP (CON-data-availability — self-recorded is the clean legal path): obtain a 5-15s
       single-possession .mp4, stable camera, wide half-court side angle, >=720p. Self-record
       at a gym/court (recommended) OR trim a short clip for LOCAL/PRIVATE demo only (do not
       distribute). Upload to Colab as /content/clip.mp4.
    2. GPU: Colab -> Runtime -> Change runtime type -> T4 GPU -> Save. Confirm
       `import torch; torch.cuda.is_available()` is True.
    3. API KEY: create a Roboflow API key; add it as a Colab Secret named ROBOFLOW_API_KEY
       (key icon in the left panel). DO NOT paste it into a cell. Confirm
       `from google.colab import userdata; userdata.get('ROBOFLOW_API_KEY')` returns a value.
    4. KEYPOINT ORDERING (resolves RESEARCH Open Question 1): run the court keypoint model
       (basketball-court-detection-2) on one frame of the clip in a scratch cell; print
       `keypoints.xy[0].shape` and the per-index confidence. Map each detected index to a
       court feature and confirm it lines up with KEYPOINT_NAMES in basketball_court_config.py
       (reorder/subselect the vertex array in cell 3 to match — apply the SAME confidence mask
       `kp.xy[0][:,0] > 1` to BOTH source and target, per Pitfall 7).
  </how-to-verify>
  <acceptance_criteria>
    - A `/content/clip.mp4` exists in the Colab session (5-15s, plays, half-court visible).
    - `torch.cuda.is_available()` prints True (T4 active).
    - `userdata.get('ROBOFLOW_API_KEY')` returns a non-empty string AND the key appears nowhere in any saved notebook cell (grep the saved .ipynb for the literal key -> must be 0 hits).
    - A scratch-cell printout records the court model's keypoint count + index->feature mapping, and the cell-3 target vertices are confirmed reordered to match KEYPOINT_NAMES.
  </acceptance_criteria>
  <resume-signal>Reply "ready: clip + T4 + key + keypoint order confirmed" (paste the keypoint count/shape), or describe what is blocking.</resume-signal>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Fill Colab cell 3 (RF-DETR + SAM2 + teams + jersey + homography)</name>
  <read_first>
    - read_the_floor/colab_read_the_floor.ipynb (cell 6 = the SKELETON to replace; cells 8 and 10 stay UNCHANGED except the added inline asserts in cell 8)
    - .planning/phases/01-video-coordinates/01-RESEARCH.md ("Recommended Colab cell 3 structure", all 7 Pitfalls, "Don't Hand-Roll", "Output Contract")
    - read_the_floor/basketball_court_config.py (vertices_ft, KEYPOINT_NAMES)
    - read_the_floor/adapters/skalski.py (the contract frames_out must satisfy — DO NOT edit)
  </read_first>
  <action>
    Edit `read_the_floor/colab_read_the_floor.ipynb` — replace the cell whose source begins
    `# ---- SKELETON: fill from Skalski's notebook ----` (the cell-3 code cell) with the full
    cell-3 pipeline following RESEARCH "Recommended Colab cell 3 structure" exactly. Concrete
    requirements:

    0. Import `import basketball_court_config as bcc` (the read_the_floor folder is already on
       sys.path from the modules cell's `sys.path.insert`). Set `TARGET = bcc.vertices_ft()` (FEET).

    1. Models (use the Roboflow inference SDK as primary per RESEARCH Open Question 3):
       `from inference import get_model`
       `player_model = get_model("basketball-player-detection-3-ycjdo/<ver>", api_key=userdata.get('ROBOFLOW_API_KEY'))`
       `court_model  = get_model("basketball-court-detection-2/<ver>", api_key=...)`
       Fallback noted in a comment: direct `from rfdetr import RFDETRBase` if get_model is brittle.

    2. TeamClassifier — fit ONCE (Pitfall 3): stride-sample crops with
       `sv.get_video_frames_generator(VIDEO, stride=30)`, detect players (threshold ~0.3,
       filter to the `player` class), `sv.crop_image`, then
       `tc = TeamClassifier(device='cuda'); tc.fit(crops)`.

    3. SAM2 tracking (Pitfall 2 + Pitfall 6):
       `from sam2.build_sam import build_sam2_video_predictor` (fallback comment: git install
       facebookresearch/sam2 if the PyPI package lacks it, per RESEARCH Open Question 2).
       Init from frame-0 RF-DETR player boxes as prompts; `propagate_in_video`. When building
       `players_sv` from masks, set `players_sv.tracker_id = np.array(out_obj_ids)` (MUST NOT be
       None). Clean masks: keep largest connected component, discard masks whose bbox area
       > 20% of frame (Pitfall 6). Use the box/mask BOTTOM-center as ground contact (the adapter
       does this; do not pass centroids — Pitfall: mask centroid inflates height).

    4. Per-frame homography (Pitfall 4 + Pitfall 7):
       `kp = sv.KeyPoints.from_inference(court_model.infer(frame)[0])`
       `mask = kp.xy[0][:,0] > 1`
       `transformer = ViewTransformer(source=kp.xy[0][mask].astype(np.float32), target=TARGET[mask].astype(np.float32))`
       Wrap in try/except; if <4 keypoints above conf 0.5 or homography raises, REUSE the
       previous frame's transformer (do not crash, do not emit garbage).

    5. Team per frame: `labels = tc.predict(crops_frame)`;
       `team_by_id = {int(tid): int(l) for tid,l in zip(players_sv.tracker_id, labels)}`.

    6. Ball: re-detect each frame at LOWER confidence ~0.2 (Pitfall 5); `ball_sv` may be empty.

    7. Jersey OCR: OPTIONAL — leave `number_by_id = {}` (brain tolerates empty identity). If
       time allows, SmolVLM2 voted across sampled frames cached by tracker_id (Pitfall: do not
       run every frame).

    8. Assemble `frames_out.append((players_sv, ball_sv, transformer, team_by_id, number_by_id))`.

    DO NOT edit the adapt cell's existing logic (cell beginning `per_frame = []`) or the brain
    cell (`results, off_team, hoop = brain.run(df)`) — adapter + brain + render are the
    unchanged path (CON-schema-contract, DEC-pipeline-fork: fork the pipeline, the value layer
    bolts on AFTER frame_to_rows). DO NOT fork schema.py. DO NOT hardcode the API key in any
    cell (read from userdata/Colab secret).

    Then ADD the 6 inline sanity asserts to the adapt cell (the one with
    `df = skalski.build_table(...)`) right after `schema.validate(df)`, copied from RESEARCH
    "Phase 1 Requirements -> Test Map":
    ```python
    assert df[~df.is_ball].x.between(0,94).all() and df[~df.is_ball].y.between(0,50).all(), "FEET trap: coords out of court"
    assert (df.groupby('frame').size() >= 2).mean() > 0.9, "too few rows/frame"
    assert df.is_ball.mean() > 0.1, "ball missing >90% of frames"
    stable = (df[~df.is_ball].groupby('track_id').frame.count() > len(df.frame.unique())*0.5).sum()
    assert stable >= 2, "no stable tracks (SAM2 ids not wired?)"
    ```
  </action>
  <verify>
    <automated>cd "read_the_floor" && python3 -c "import json; nb=json.load(open('colab_read_the_floor.ipynb')); src=''.join(''.join(c.get('source',[])) for c in nb['cells']); flat=src.replace(' ',''); checks={'frames_out':'frames_out' in src,'feet_target':('vertices_ft' in src or 'BASKETBALL_COURT_VERTICES_FT' in src),'tracker_id':'tracker_id' in src,'TeamClassifier':'TeamClassifier' in src,'ViewTransformer':'ViewTransformer' in src,'feet_assert':'between(0,94)' in flat,'adapter_unchanged_call':'skalski.frame_to_rows' in src,'render_call':'render.render_gif' in src}; missing=[k for k,v in checks.items() if not v]; assert not missing, 'MISSING: '+str(missing); print('notebook cell-3 structure OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: notebook contains frames_out, uses vertices_ft/feet target, wires tracker_id, uses TeamClassifier + ViewTransformer, keeps skalski.frame_to_rows + render.render_gif (adapter/brain/render UNCHANGED), and has the between(0,94) feet-trap assert in the adapt cell.
    - `grep -cE "ROBOFLOW_API_KEY[\"']?[[:space:]]*[:=][[:space:]]*[\"'][A-Za-z0-9]" read_the_floor/colab_read_the_floor.ipynb` == 0 (no hardcoded key literal; only `userdata.get(...)`).
    - `grep -c "userdata.get\|os.environ" read_the_floor/colab_read_the_floor.ipynb` >= 1 (secret read from environment/Colab secret).
    - The skalski adapter file is unchanged (the seam is the contract, not edited): `git diff --quiet read_the_floor/adapters/skalski.py` exits 0.
    - schema.py is unchanged: `git diff --quiet read_the_floor/schema.py` exits 0 (no schema fork).
  </acceptance_criteria>
  <done>Cell 3 is the full RF-DETR/SAM2/teams/homography pipeline targeting feet via BasketballCourtConfig, frames_out assembled, SAM2 tracker_ids wired, inline feet/geometry asserts added; adapter + brain + render + schema all unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run on one real clip -> X-ray GIF + sanity vs SportVU; save artifacts</name>
  <read_first>
    - read_the_floor/SETUP.md (Track B B4-B6)
    - read_the_floor/validate_video.py and run_video.py (the local report/render tools from plan 02 — run these on the exported frames_out)
    - .planning/phases/01-video-coordinates/01-RESEARCH.md ("Sanity Checks: Video-Derived Coords vs SportVU Baseline (SC4)", "Validation Architecture" sign-off)
  </read_first>
  <what-built>
    Cell 3 is filled. Now run cells 1-5 on the real /content/clip.mp4 in the T4 session,
    confirm the inline asserts pass, eyeball the overlay, and persist the artifacts.
  </what-built>
  <how-to-verify>
    1. Runtime = T4 GPU. Run the install, modules, and cell-3 pipeline cells on
       /content/clip.mp4. Cell 3 completes with no exception and `len(frames_out) > 0`.
    2. Run the adapt cell: `schema.validate(df)` passes AND all 6 inline asserts pass
       (x in [0,94], y in [0,50], >=2 rows/frame on >90% frames, ball >10%, >=2 stable tracks).
    3. Run the brain+render cell: `brain.run(df)` completes; the overlay GIF renders inline.
    4. SANITY vs SportVU (SC4): in Colab, `pickle.dump(frames_out, open('/content/frames_out.pkl','wb'))`,
       download it, and locally run
       `python3 validate_video.py --frames-out frames_out.pkl` — confirm median hoop distance is
       5-40 ft (~18 SportVU baseline), control share > 0.6, beaten_std > 5 (PASS verdicts), and
       the valid_rate/swap_rate/contain_rate are in a plausible neighborhood of the SportVU
       numbers (validate.py on a SportVU game gives valid_rate ~1.0, swap ~low, beaten mean ~55).
       Visually spot-check 5 overlay stills against the source frames (player dots match positions).
    5. SAVE ARTIFACTS: render the GIF to `prototypes/video_matchup.gif` and dump stills to
       `read_the_floor/frames/` (use `run_video.py --frames-out frames_out.pkl --out
       prototypes/video_matchup.gif --stills 5 --frames-dir read_the_floor/frames`).
    6. Update SETUP.md Track B (B5) with the one or two clip-specific gotchas you hit (e.g. the
       confirmed keypoint count, the SAM2 install path that worked) — surgical, a few lines.
  </how-to-verify>
  <acceptance_criteria>
    - `prototypes/video_matchup.gif` exists and is non-empty (`test -s prototypes/video_matchup.gif`).
    - `read_the_floor/frames/` contains >=3 `still_*.png` files from the video run.
    - `python3 read_the_floor/validate_video.py --frames-out <downloaded frames_out.pkl>` prints all PASS on the SC4 block (or documented WARNs with a one-line explanation in the SUMMARY), exit 0.
    - In-notebook: the adapt cell's `schema.validate(df)` + the 6 inline asserts pass on the REAL clip (paste the cell output into the resume signal).
    - SETUP.md Track B updated with the clip-run specifics (`git diff --stat read_the_floor/SETUP.md` shows it changed).
  </acceptance_criteria>
  <resume-signal>Reply "rendered: prototypes/video_matchup.gif + N stills, SC4 PASS" and paste the validate_video.py output + the adapt-cell assert output. If a sanity check WARNs, describe it and whether it blocks.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Roboflow API key | Secret crosses into Colab; must come from a Colab Secret / env var, never a cell literal |
| Uploaded video clip -> CV pipeline | A video file (possibly externally sourced) is decoded and inferred on |
| Downloaded model weights | RF-DETR / SAM2 / SigLIP / SmolVLM2 weights fetched from PyPI/HF/Roboflow |
| frames_out pickle (Colab -> local) | Self-produced artifact moved between the two environments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information disclosure | ROBOFLOW_API_KEY in notebook | mitigate | Key read ONLY via `userdata.get('ROBOFLOW_API_KEY')` (Colab Secret); acceptance greps the saved .ipynb for any key literal -> must be 0; never committed to git |
| T-01-08 | Spoofing / supply chain | model weights provenance | mitigate | Pin versions (rfdetr 1.3.0, supervision 0.28.0, umap-learn 0.5.12, sports@42c80c0); install only from PyPI / official roboflow + facebookresearch repos named in RESEARCH; no arbitrary URLs |
| T-01-09 | Tampering | untrusted video file decode | accept | Decoded by standard cv2/supervision in an ephemeral Colab VM (sandboxed, disposable); clip is operator-chosen; low-value local target (ASVS L1) |
| T-01-10 | Elevation / RCE | pickle.load of frames_out locally | mitigate | Load only the operator's OWN exported frames_out (documented); never a third-party pickle; proportionate to a local research pipeline |
| T-01-11 | Repudiation | clip licensing (CON-data-availability) | mitigate | Prefer self-recorded footage (clean legal path); any trimmed clip used for LOCAL/PRIVATE demo only, not distributed — documented in SETUP.md and the checkpoint |
</threat_model>

<verification>
- Cell 3 runs on /content/clip.mp4 producing `frames_out` (len > 0) with no exception (manual, Colab T4).
- The adapt cell: `schema.validate(df)` + 6 inline geometry/presence asserts pass on the real clip.
- The brain+render cell: `brain.run(df)` + `render.render_gif` produce the overlay GIF inline.
- `python3 read_the_floor/validate_video.py --frames-out frames_out.pkl` — SC4 sanity PASS vs SportVU baseline.
- `test -s prototypes/video_matchup.gif` and `ls read_the_floor/frames/still_*.png` — artifacts persisted.
- `git diff --quiet read_the_floor/adapters/skalski.py read_the_floor/schema.py read_the_floor/brain.py read_the_floor/render.py` — the seam contract + brain + render UNCHANGED (only the notebook + SETUP.md edited).
</verification>

<success_criteria>
- One real clip flows video -> coordinates -> brain -> broadcast X-ray overlay GIF (the four ROADMAP success criteria).
- No schema fork; the adapter is the only seam; brain + render run unchanged.
- Coordinates are in FEET (0..94 x 0..50); SC4 numbers are plausible vs the SportVU baseline.
- The headline GIF is in prototypes/ and stills in read_the_floor/frames/.
</success_criteria>

<output>
After completion, create `.planning/phases/01-video-coordinates/01-03-SUMMARY.md`.
</output>
